import { colors } from "../constants/colors";
import { spacing } from "../constants/spacing";

export const theme = {
  colors,
  spacing,
  radius: {
    sm: 10,
    md: 16,
    lg: 22,
    xl: 28,
  },
};